# This is a basic workflow to help you get started with Actions

name: Snake Game

# Controlador do tempo que sera feito a atualização dos arquivos.
on:
  schedule:
      # Será atualizado a cada 5 horas.
    - cron: "0 */5 * * *"

# Permite executar na na lista de Actions (utilizado para testes de build).
  workflow_dispatch:

# Regras
jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    # Checks repo under $GITHUB_WORKSHOP, so your job can access it
      - uses: actions/checkout@v2

    # Repositorio que será utilizado para gerar os arquivos.
      - uses: Platane/snk@master
        id: snake-gif
        with:
          github_user_name: nomeUsuario #Seu usuario
          gif_out_path: dist/github-contribution-grid-snake.gif
          svg_out_path: dist/github-contribution-grid-snake.svg

      - run: git status

      # Para as atualizações.
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: master
          force: true

      - uses: crazy-max/ghaction-github-pages@v2.1.3
        with:
          # the output branch we mentioned above
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
Retornando na opção Actions do repositório, clique em “Run workflow”:

repositório

Pasta dos arquivos:
Em seu repositório cliente em main e selecione a saída branch:
resultado

E aqui estão os arquivos gerados que você pode usar como bem entender:

Arquivos

Codigo Github para uso:

![snake gif](https://github.com/SEU_USUARIO/SEU_REPOSITORIO/blob/output/github-contribution-grid-snake.svg)

Principais comentários (0)

Se inscrever
foto
Adicionar à discussão
Código de Conduta • Relatar abuso
👋 Ei, meu nome é Noah e fui eu que montei esse anúncio. Meu trabalho é fazer com que você se junte ao DEV, então se quiser me fazer um favor, adoraria que você criasse uma conta.
Se você encontrou DEV pesquisando, aqui estão alguns de nossos artigos mais populares sobre DEV:

9 projetos que você pode fazer para se tornar um frontend master
101 Tips For Being A Great Programmer
Full Stack Developer’s Roadmap
Read next
imagem de perfil brunobrito
Why You Should Use a Git Client
Bruno Brito - Nov 22

imagem de perfil manthanank
GitHub Most Used Repositories : Part 1
Manthan Ank - Nov 27

imagem de perfil de mbarzeev
Visual Testing Your Components With Chromatic
Matti Bar-Zeev - Nov 25

imagem de perfil da abadia
#gitPanic - Removing and Restoring Work
Abbey Perini - Nov 22


Henrique Lopes
Follow
"Não há razão alguma em usar a palavra “impossível” para descrever algo que claramente pode acontecer."
JOINED
5 de set. de 2021
Trending on DEV Community 👩‍💻👨‍💻 
Michael Tharrington (ele/ele) imagem de perfil
What was your win this week?
#discuss #weeklyretro
imagem de perfil Sherry Day
What are you currently working on? What's the most interesting part?
# discutir
imagem de perfil Kayode
Como eu uso o Notion como desenvolvedor de software
# produtividade # iniciantes # noção # 100daysofcode
# Nome da Actions:  
name: Snake Game

# Controlador do tempo que sera feito a atualização dos arquivos.
on:
  schedule:
      # Será atualizado a cada 5 horas.
    - cron: "0 */5 * * *"

# Permite executar na na lista de Actions (utilizado para testes de build).
  workflow_dispatch:

# Regras
jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    # Checks repo under $GITHUB_WORKSHOP, so your job can access it
      - uses: actions/checkout@v2

    # Repositorio que será utilizado para gerar os arquivos.
      - uses: Platane/snk@master
        id: snake-gif
        with:
          github_user_name: nomeUsuario #Seu usuario
          gif_out_path: dist/github-contribution-grid-snake.gif
          svg_out_path: dist/github-contribution-grid-snake.svg

      - run: git status

      # Para as atualizações.
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: master
          force: true

      - uses: crazy-max/ghaction-github-pages@v2.1.3
        with:
          # the output branch we mentioned above
          target_branch: output
          build_dir: dist
